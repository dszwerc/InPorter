import SwiftUI

struct InPorterTheme {
    static let accentColor = Color.accentColor
    static let bannerBackground = Color(NSColor.windowBackgroundColor)
    static let bannerBorder = Color.secondary.opacity(0.4)
}
